## Goal
Two deliverables:
1. **Remove real example + password auth** — replace the real personal GEDCOM (`family-tree.ged`, real Trần family data) with the old generic example, and strip out all client-side passcode/password authentication so the app loads the default example directly.
2. **Fix all CVEs** — run `npm audit fix` to clear the 7 reported vulnerabilities (vite, postcss, lodash-es, immutable, brace-expansion, picomatch, @babel/core).

## Findings
- **Real example**: `src/gedcoms/family-tree.ged` contains real personal data (Phú Trần Thiện and family). The original "old example" (commit `c2f13b0`, deleted later) was `public/gedcom/family.ged` — a generic John/Jane/Baby Doe tree. I'll restore that generic content as the default example.
- **Auth wiring** (client-side passcode) spans: `src/index.jsx` (gate + `PasscodeModal`), `src/Load.jsx` (`isAuthenticated` gating + auto-load), `src/Controls.jsx` (`LogoutButton`), `src/stores/authStore.js`, the auth components (`PasscodeModal.jsx`, `LogoutButton.jsx`+scss), the passcode scss + its `@use`, `vite.config.js` define block, `_headers`/`.env`/`.env.example`, and i18n `auth` keys.
- **Audit**: all 7 vulns report `fix available via npm audit fix`.

## Plan

### Part 1 — Replace real example with old generic example
- `src/gedcoms/family-tree.ged`: replace the real Trần family data with the generic John/Jane/Baby Doe tree (the content that was the original `public/gedcom/family.ged`). Keeps the import path in `Load.jsx`/`index.jsx` unchanged.

### Part 2 — Remove client-side password auth
**`src/index.jsx`**
- Remove imports of `useAuthStore` and `PasscodeModal`.
- Remove `isClientSideAuthEnabled`, `isAuthenticated`, the auth subscription `useEffect`, and `shouldShowAppContent`.
- Remove the `{isClientSideAuthEnabled && <PasscodeModal />}` line and the `{shouldShowAppContent && (...)}` wrapper — render app content directly.
- Drop `isAuthenticated={isAuthenticated}` from `<Load>`.

**`src/Load.jsx`**
- Remove `isAuthenticated` prop, the early-return guard, the `isAuthenticated` deps, and all `action-card-disabled` / `isAuthenticated &&` gating so actions work unconditionally. Auto-load still runs on first session load.

**`src/Controls.jsx`**
- Remove `LogoutButton` import and the `<LogoutButton />` settings block (+ its surrounding divider/row) so the settings panel has no orphaned logout entry.

**Delete files**
- `src/stores/authStore.js`
- `src/components/auth/PasscodeModal.jsx`
- `src/components/auth/LogoutButton.jsx`
- `src/components/auth/LogoutButton.scss`
- `src/components/auth/` (if empty)
- `src/sass/components/auth/passcode-modal.scss`
- `src/sass/components/auth/` (if empty)
- Remove `@use 'components/auth/passcode-modal';` from `src/sass/style.scss`.

**Config / env**
- `vite.config.js`: remove the two `import.meta.env.VITE_PASSCODE` / `ENABLE_CLIENT_SIDE_AUTH` define lines.
- `public/_headers`: update the comment that says "Using client-side passcode only." to reflect no auth.
- `.env` / `.env.example`: remove the `ENABLE_CLIENT_SIDE_AUTH` and `VITE_PASSCODE` blocks (keep the file header / reserved flags section).

**i18n**
- Remove the `auth` block (`passcode` + `logout`) from both `src/i18n/locales/en/translation.json` and `src/i18n/locales/vi/translation.json`.

(Note: `_bmad*` / `_bmad-output` are gitignored historical planning docs — leaving untouched. `home.discoverRoots*` keys stay since "Discover Your Roots" still loads the default example.)

### Part 3 — Audit & CVE fix
- Run `npm audit fix` to auto-upgrade the fixable packages (vite, postcss, lodash-es, immutable, brace-expansion, picomatch, @babel/core).
- Re-run `npm audit` to confirm 0 vulnerabilities.
- Verify the app still builds: `npm run build`.

## Verification checklist
- [ ] `src/gedcoms/family-tree.ged` contains generic John/Jane/Baby Doe data, no real names.
- [ ] No remaining references to passcode/auth stores/components in `src/`.
- [ ] `npm audit` reports 0 vulnerabilities.
- [ ] `npm run build` succeeds.
- [ ] No orphaned imports/props (grep clean for `isAuthenticated`, `PasscodeModal`, `LogoutButton`, `authStore`).